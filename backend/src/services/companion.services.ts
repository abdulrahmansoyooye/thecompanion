import { prisma } from "../lib/prisma.ts"

export const ComapnionServices = {
   async create(usrId:string, payload:any){
      // return prisma.
   },
   async list(){
      // return 
   },
   async getOne(){

   },
   async update(){

   },
   async remove(){
      
   }
}